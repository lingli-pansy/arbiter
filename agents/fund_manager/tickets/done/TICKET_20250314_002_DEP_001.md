# TICKET_20250314_002_DEP_001

## Source Task
TASK_20250314_002_IB_ENVIRONMENT_SETUP - IB 环境连接验证失败

## Blocking Issue
缺少 Python 依赖 `ib_insync`，导致无法连接 IB。

错误信息:
```
ib_insync not installed: No module named 'ib_insync'
```

## Requested Capability
安装 `ib_insync` Python 包，使 IB 连接工具可正常工作。

## Why This Is Required
- `connect_broker` 和 `get_broker_positions` 等工具依赖 `ib_insync` 库
- 这是 Interactive Brokers API 的异步 Python 封装
- 这是与 IB Gateway/TWS 通信的标准库

## Installation
```bash
pip install ib_insync
```

可选 (推荐) 创建 requirements.txt:
```
ib_insync>=0.9.70
yfinance>=0.2.28  # 可选，如仍需 Yahoo 备用
pandas>=1.5.0
```

## Acceptance Criteria
1. `pip install ib_insync` 成功
2. 验证安装:
   ```bash
   python3 -c "import ib_insync; print(ib_insync.__version__)"
   ```
3. 重新运行连接测试:
   ```bash
   cd /Users/xiaoyu/arbiter-2/system/tools/impl
   echo '{"broker": "ib", "mode": "paper"}' | python3 connect_broker.py
   ```
4. 期望输出:
   ```json
   {
     "success": true,
     "status": "connected",
     "connection_id": "ib_paper_xxx",
     "latency_ms": 120
   }
   ```

## Test Case
```bash
# 安装后验证
python3 -c "from ib_insync import IB; print('ib_insync OK')"

# 连接测试
cd /Users/xiaoyu/arbiter-2/system/tools/impl
echo '{"broker": "ib", "mode": "paper"}' | python3 connect_broker.py
```

## Related
- Original Task: TASK_20250314_002_IB_ENVIRONMENT_SETUP
- Blocking: TASK_20250314_001_IB_MARKET_CAP_ARCH_FIX
- Validation: VALIDATION_20250314_002_CONNECTION.md

## Priority
P0 - 阻塞所有 IB 相关能力

## Status
Open

## Created
2026-03-14
