---
id: REPORT_20250314_TICKET_001_FIX
date: 2025-03-14
type: fix
status: completed
---

# TICKET_20250314_001 修复报告

## 问题描述
TICKET_20250314_001 被标记为 `done`，但实际上 Contract 与 Ticket 要求不匹配：
- Ticket 要求参数名为 `source`，Contract 实际为 `provider`
- Ticket 要求枚举值 `["yahoo", "ib", "polygon", "alpaca"]`，实际为 `["yahoo", "ibkr", "polygon"]`
- Ticket 要求返回 `latency_ms` 和 `data_quality_score`，Contract 未定义

## 修复内容

### 1. Ticket 状态更新
- **从**: `tickets/done/TICKET_20250314_001_source_param.md` (status: done)
- **移动到**: `tickets/open/TICKET_20250314_001_source_param.md` (status: open)
- **更新内容**: 添加具体的修复要求和验收标准

### 2. Contract 更新 (`system/tools/contracts/get_market_bars_batch.yaml`)
```yaml
input:
  source:  # 新增，优先于 provider
    type: string
    enum: ["yahoo", "ib", "polygon", "alpaca"]
    default: "yahoo"
  provider:  # 标记为 deprecated，向后兼容
    type: string
    deprecated: true

output.meta:
  source: { type: string }
  latency_ms: { type: integer }
  data_quality_score: { type: number }
```

### 3. Implementation 更新 (`system/tools/impl/get_market_bars_batch.py`)
- 添加 `_get_source()` 函数处理参数映射
- 支持 `source` 参数（新）和 `provider` 参数（向后兼容）
- 映射 `"ib"` → `"ibkr"` 用于 yfinance
- 添加延迟测量 (`time.time()`)
- 添加数据质量评分计算 (`_calculate_data_quality()`)

## 验证结果

### Test 1: 新参数 `source`
```bash
python3 get_market_bars_batch.py '{"symbols": ["AAPL"], "lookback_days": 5, "source": "yahoo"}'
```
**结果**: ✅
```json
{
  "success": true,
  "meta": {
    "source": "yahoo",
    "latency_ms": 1686,
    "data_quality_score": 1.0
  }
}
```

### Test 2: 向后兼容 `provider`
```bash
python3 get_market_bars_batch.py '{"symbols": ["AAPL"], "lookback_days": 5, "provider": "yahoo"}'
```
**结果**: ✅
```json
{
  "success": true,
  "meta": {
    "source": "yahoo",
    "latency_ms": 1803,
    "data_quality_score": 1.0
  }
}
```

### Test 3: IB source 映射
```bash
python3 get_market_bars_batch.py '{"symbols": ["AAPL"], "lookback_days": 5, "source": "ib"}'
```
**结果**: ✅ (正确映射为 yfinance 的 ibkr)

## 当前状态

| 项目 | 状态 |
|------|------|
| Contract 更新 | ✅ 已完成 |
| Implementation 更新 | ✅ 已完成 |
| Ticket 位置 | ✅ `tickets/open/` |
| 向后兼容 | ✅ `provider` 参数仍可用 |
| 延迟测量 | ✅ `latency_ms` 已添加 |
| 数据质量 | ✅ `data_quality_score` 已添加 |

## 待办事项

Ticket 保持在 `open` 状态，待以下能力实现后可关闭：
- [ ] `polygon` 数据源支持
- [ ] `alpaca` 数据源支持
- [ ] 真正的 `ib` (Interactive Brokers) 数据源支持（非 yfinance 映射）

---
Generated by: arbiter-dev
Date: 2025-03-14
